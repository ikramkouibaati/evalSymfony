{{ form_start(form) }}
    {{ form_row(form.titre) }}
    {{ form_row(form.marque) }}
    {{ form_row(form.modele) }}
    {{ form_row(form.description) }}
    {{ form_row(form.photo) }}
{% if vehicule.photo != null %}
    <p>Photo actuel de la voiture : </p>
    <img src="{{ asset(vehicule.photo) }}" alt="Aperçu de l'image par défaut" class="imageIndex mb-3 ms-3"/>
{% endif %}
    {{ form_row(form.prix_journalier) }}
    {{ form_row(form.submit) }}
{{ form_end(form) }}