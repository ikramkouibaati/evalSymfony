{% extends 'back.html.twig' %}

{% block title %}Modifier Vehicule{% endblock %}

{% block bodyback %}
    <h1>Modifier Vehicule</h1>

    {{ include('vehicule/_form.html.twig', {'button_label': 'Modifier'}) }}

    <a href="{{ path('app_vehicule_index') }}" class="btn btn-secondary btnRetour">Retour a la liste</a>

    {{ include('vehicule/_delete_form.html.twig') }}
{% endblock %}
